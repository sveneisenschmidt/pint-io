PHP Extensions via PECL
=======================

* pecl_http

SapiAdapter
----------_
* runkit
    - only working version comes from github, download the tgz file
      and install it via (sudo) pecl install /path/to/runkit_package.tgz 
